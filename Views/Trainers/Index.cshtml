@model IEnumerable<WebProgramlamaProje.Models.Trainer>

@{
    ViewData["Title"] = "Eğitmenlerimiz";
}

<div class="page-header mb-5">
    <div class="d-flex justify-content-between align-items-center">
        <div>
            <h1 class="display-5 fw-bold gradient-text mb-2">Eğitmenlerimiz</h1>
            <p class="lead text-secondary">Profesyonel ve sertifikalı uzman kadromuz</p>
        </div>
        @if (User.IsInRole("Admin"))
        {
            <a asp-action="Create" class="btn btn-primary">
                <span class="me-2">➕</span> Yeni Eğitmen Ekle
            </a>
        }
    </div>
</div>

<div class="row g-4">
    @foreach (var item in Model)
    {
        <div class="col-md-6 col-lg-4">
            <div class="card trainer-card h-100">
                <div class="trainer-avatar-section">
                    <div class="avatar-circle">
                        <span class="avatar-icon">👤</span>
                    </div>
                    <div class="trainer-header-info mt-3">
                        <h4 class="trainer-name mb-1">@Html.DisplayFor(modelItem => item.Name)</h4>
                        <div class="trainer-specialty">
                            <span class="specialty-badge">@Html.DisplayFor(modelItem => item.Specialty)</span>
                        </div>
                    </div>
                </div>
                
                <div class="card-body d-flex flex-column">
                    <div class="trainer-gym mb-3">
                        <span class="gym-icon">🏋️</span>
                        <span class="gym-name">@item.Gym?.Name</span>
                    </div>
                    
                    <div class="trainer-description mb-3">
                        <p class="description-text">@Html.DisplayFor(modelItem => item.Description)</p>
                    </div>
                    
                    <div class="card-actions mt-auto">
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info btn-sm flex-fill">
                            <span class="me-1">📋</span> Profil
                        </a>
                        
                        @if (User.IsInRole("Admin"))
                        {
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning btn-sm flex-fill">
                                <span class="me-1">✏️</span> Düzenle
                            </a>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm flex-fill">
                                <span class="me-1">🗑️</span> Sil
                            </a>
                        }
                    </div>
                </div>
            </div>
        </div>
    }
</div>

@if (!Model.Any())
{
    <div class="empty-state text-center py-5">
        <div class="empty-icon mb-3">💪</div>
        <h3 class="text-muted">Henüz Eğitmen Eklenmemiş</h3>
        <p class="text-secondary">İlk eğitmeni eklemek için yukarıdaki butonu kullanın.</p>
    </div>
}

<style>
    .page-header {
        padding-bottom: 1.5rem;
        border-bottom: 1px solid rgba(0, 245, 255, 0.2);
    }

    .gradient-text {
        background: linear-gradient(135deg, var(--neon-cyan), var(--neon-magenta));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .trainer-card {
        transition: all 0.3s ease;
        overflow: hidden;
    }

    .trainer-avatar-section {
        padding: 2rem;
        background: linear-gradient(135deg, var(--surface-primary), var(--surface-elevated));
        text-align: center;
        border-bottom: 1px solid rgba(0, 245, 255, 0.2);
    }

    .avatar-circle {
        width: 100px;
        height: 100px;
        margin: 0 auto;
        border-radius: 50%;
        background: linear-gradient(135deg, rgba(0, 245, 255, 0.2), rgba(255, 0, 170, 0.2));
        border: 3px solid rgba(0, 245, 255, 0.3);
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 0 20px rgba(0, 245, 255, 0.3);
    }

    .avatar-icon {
        font-size: 3rem;
    }

    .trainer-header-info {
        padding-bottom: 0.5rem;
    }

    .trainer-name {
        color: var(--neon-cyan);
        font-weight: 700;
        font-size: 1.25rem;
        margin-bottom: 0.5rem;
    }

    .trainer-specialty {
        display: inline-block;
    }

    .specialty-badge {
        background: linear-gradient(135deg, rgba(0, 245, 255, 0.15), rgba(255, 0, 170, 0.15));
        color: var(--neon-cyan);
        padding: 0.35rem 1rem;
        border-radius: 20px;
        font-size: 0.85rem;
        font-weight: 600;
        border: 1px solid rgba(0, 245, 255, 0.3);
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .trainer-gym {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.75rem;
        background: rgba(0, 245, 255, 0.05);
        border-radius: 8px;
        border-left: 3px solid var(--power-green);
    }

    .gym-icon {
        font-size: 1.5rem;
        flex-shrink: 0;
    }

    .gym-name {
        color: var(--text-secondary-color);
        font-weight: 600;
        font-size: 0.95rem;
    }

    .trainer-description {
        flex: 1;
    }

    .description-text {
        color: var(--text-secondary-color);
        font-size: 0.9rem;
        line-height: 1.6;
        margin: 0;
    }

    .card-actions {
        display: flex;
        gap: 0.5rem;
        padding-top: 1rem;
        border-top: 1px solid rgba(0, 245, 255, 0.1);
    }

    .card-actions .btn {
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 0.6rem 1rem;
        font-size: 0.85rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .empty-state {
        background: rgba(36, 40, 66, 0.3);
        border: 2px dashed rgba(0, 245, 255, 0.2);
        border-radius: 12px;
        margin-top: 3rem;
    }

    .empty-icon {
        font-size: 4rem;
        opacity: 0.3;
    }

    /* Responsive */
    @@media (max-width: 768px) {
        .avatar-circle {
            width: 80px;
            height: 80px;
        }

        .avatar-icon {
            font-size: 2.5rem;
        }

        .card-actions {
            flex-direction: column;
        }

        .card-actions .btn {
            width: 100%;
        }
    }
</style>
