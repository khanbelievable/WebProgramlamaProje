@model WebProgramlamaProje.Models.Trainer

@{
    ViewData["Title"] = "Eğitmen Detayları";
}

<div class="row justify-content-center">
    <div class="col-md-10">
        <div class="card trainer-details-card">
            <div class="trainer-header">
                <div class="avatar-circle-large">
                    <span class="avatar-icon">👤</span>
                </div>
                <h2 class="trainer-name-large mt-3">@Model.Name</h2>
                <div class="specialty-badge-large">@Model.Specialty</div>
                
                @if (User.IsInRole("Admin"))
                {
                    <div class="mt-3">
                        <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning btn-sm">
                            <span class="me-1">✏️</span> Düzenle
                        </a>
                        <a asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger btn-sm">
                            <span class="me-1">🗑️</span> Sil
                        </a>
                    </div>
                }
            </div>
            
            <div class="card-body">
                <div class="trainer-info-grid">
                    <div class="info-card">
                        <span class="info-icon">🏋️</span>
                        <div>
                            <strong>Çalıştığı Salon</strong>
                            <p>@Model.Gym?.Name</p>
                        </div>
                    </div>
                    
                    <div class="info-card full-width">
                        <span class="info-icon">📝</span>
                        <div>
                            <strong>Hakkında</strong>
                            <p>@Model.Description</p>
                        </div>
                    </div>
                </div>
                
                <div class="mt-4">
                    <a asp-action="Index" class="btn btn-secondary">
                        <span class="me-2">↩️</span> Eğitmen Listesine Dön
                    </a>
                    <a asp-controller="Appointments" asp-action="Create" asp-route-trainerId="@Model.Id" class="btn btn-success">
                        <span class="me-2">📅</span> Randevu Al
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .trainer-details-card {
        overflow: hidden;
    }

    .trainer-header {
        padding: 3rem 2rem;
        background: linear-gradient(135deg, var(--surface-primary), var(--surface-elevated));
        text-align: center;
        border-bottom: 1px solid rgba(0, 245, 255, 0.2);
    }

    .avatar-circle-large {
        width: 150px;
        height: 150px;
        margin: 0 auto;
        border-radius: 50%;
        background: linear-gradient(135deg, rgba(0, 245, 255, 0.2), rgba(255, 0, 170, 0.2));
        border: 4px solid rgba(0, 245, 255, 0.4);
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 0 30px rgba(0, 245, 255, 0.4);
    }

    .avatar-circle-large .avatar-icon {
        font-size: 5rem;
    }

    .trainer-name-large {
        color: var(--neon-cyan);
        font-weight: 800;
        margin-bottom: 1rem;
    }

    .specialty-badge-large {
        display: inline-block;
        background: linear-gradient(135deg, rgba(0, 245, 255, 0.2), rgba(255, 0, 170, 0.2));
        color: var(--neon-cyan);
        padding: 0.5rem 2rem;
        border-radius: 25px;
        font-size: 1.1rem;
        font-weight: 700;
        border: 2px solid rgba(0, 245, 255, 0.4);
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .trainer-info-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 1.5rem;
        margin-top: 2rem;
    }

    .info-card {
        display: flex;
        gap: 1rem;
        padding: 1.5rem;
        background: rgba(0, 245, 255, 0.05);
        border-left: 3px solid var(--neon-cyan);
        border-radius: 8px;
    }

    .info-card.full-width {
        grid-column: 1 / -1;
    }

    .info-icon {
        font-size: 2rem;
        flex-shrink: 0;
    }

    .info-card strong {
        display: block;
        color: var(--neon-cyan);
        margin-bottom: 0.5rem;
        font-size: 0.9rem;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .info-card p {
        color: var(--text-secondary-color);
        margin: 0;
        line-height: 1.6;
    }
</style>
