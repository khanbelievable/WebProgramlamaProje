@model IEnumerable<WebProgramlamaProje.Models.Service>

@{
    ViewData["Title"] = "Hizmetler";
}

<div class="page-header mb-5">
    <div class="d-flex justify-content-between align-items-center">
        <div>
            <h1 class="display-5 fw-bold gradient-text mb-2">Hizmet Yönetimi</h1>
            <p class="lead text-secondary">Randevu için sunulan hizmetler</p>
        </div>
        <a asp-action="Create" class="btn btn-primary">
            <span class="me-2">➕</span> Yeni Hizmet Ekle
        </a>
    </div>
</div>

<div class="row g-4">
    @foreach (var item in Model)
    {
        <div class="col-md-6 col-lg-4">
            <div class="card service-card h-100">
                <div class="card-body">
                    <h4 class="service-title mb-3">@item.Name</h4>
                    
                    <p class="service-description">@item.Description</p>
                    
                    <div class="service-details mt-3">
                        <div class="detail-badge">
                            <span class="badge-icon">⏱️</span>
                            <span class="badge-text">@item.DurationMinutes dk</span>
                        </div>
                        <div class="detail-badge price">
                            <span class="badge-icon">💰</span>
                            <span class="badge-text">@item.Price.ToString("C2")</span>
                        </div>
                    </div>
                </div>
                
                <div class="card-footer">
                    <div class="d-flex gap-2">
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning btn-sm flex-fill">
                            <span class="me-1">✏️</span> Düzenle
                        </a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm flex-fill">
                            <span class="me-1">🗑️</span> Sil
                        </a>
                    </div>
                </div>
            </div>
        </div>
    }
</div>

@if (!Model.Any())
{
    <div class="empty-state text-center py-5">
        <div class="empty-icon mb-3">⚙️</div>
        <h3 class="text-muted">Henüz Hizmet Eklenmemiş</h3>
        <p class="text-secondary">İlk hizmeti eklemek için yukarıdaki butonu kullanın.</p>
    </div>
}

<style>
    .page-header {
        padding-bottom: 1.5rem;
        border-bottom: 1px solid rgba(0, 245, 255, 0.2);
    }

    .gradient-text {
        background: linear-gradient(135deg, var(--neon-cyan), var(--neon-magenta));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .service-card {
        border: 1px solid rgba(0, 245, 255, 0.2);
    }

    .service-title {
        color: var(--neon-cyan);
        font-weight: 700;
    }

    .service-description {
        color: var(--text-secondary-color);
        font-size: 0.95rem;
        line-height: 1.6;
    }

    .service-details {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .detail-badge {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.5rem 1rem;
        background: rgba(0, 245, 255, 0.1);
        border-radius: 20px;
        border: 1px solid rgba(0, 245, 255, 0.2);
    }

    .detail-badge.price {
        background: rgba(0, 255, 136, 0.1);
        border-color: rgba(0, 255, 136, 0.2);
    }

    .badge-icon {
        font-size: 1.1rem;
    }

    .badge-text {
        color: var(--text-primary-color);
        font-weight: 600;
        font-size: 0.9rem;
    }

    .card-footer {
        background: transparent;
        border-top: 1px solid rgba(0, 245, 255, 0.1);
        padding: 1rem;
    }

    .empty-state {
        background: rgba(36, 40, 66, 0.3);
        border: 2px dashed rgba(0, 245, 255, 0.2);
        border-radius: 12px;
        margin-top: 3rem;
    }

    .empty-icon {
        font-size: 4rem;
        opacity: 0.3;
    }
</style>
