@model WebProgramlamaProje.Models.Appointment

@{
    ViewData["Title"] = "Yeni Randevu Oluştur";
}

<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card appointment-form-card">
            <div class="card-header text-center py-4">
                <div class="header-icon mb-2">📅</div>
                <h2 class="gradient-text mb-0">Randevu Talebi</h2>
                <p class="text-secondary mt-2">Daha iyi bir sen için ilk adımı at</p>
            </div>
            
            <div class="card-body p-4 p-md-5">
                <form asp-action="Create" method="post">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger border-0 glow-danger mb-4"></div>
                    
                    <div class="row">
                        <div class="col-md-6 mb-4">
                            <label asp-for="TrainerId" class="form-label fw-bold mb-2">
                                <span class="me-2">💪</span> Eğitmen Seçin
                            </label>
                            <select asp-for="TrainerId" class="form-select custom-select" asp-items="ViewBag.TrainerId">
                                <option value="">Lütfen Eğitmen Seçin</option>
                            </select>
                            <span asp-validation-for="TrainerId" class="text-danger small"></span>
                        </div>
                        
                        <div class="col-md-6 mb-4">
                            <label asp-for="ServiceId" class="form-label fw-bold mb-2">
                                <span class="me-2">⚙️</span> Hizmet Seçin
                            </label>
                            <select asp-for="ServiceId" class="form-select custom-select" asp-items="ViewBag.ServiceId">
                                <option value="">Lütfen Hizmet Seçin</option>
                            </select>
                            <span asp-validation-for="ServiceId" class="text-danger small"></span>
                        </div>
                    </div>
                    
                    <div class="mb-4">
                        <label asp-for="AppointmentDate" class="form-label fw-bold mb-2">
                            <span class="me-2">🕒</span> Randevu Tarihi ve Saati
                        </label>
                        <input asp-for="AppointmentDate" class="form-control custom-input" type="datetime-local" />
                        <span asp-validation-for="AppointmentDate" class="text-danger small"></span>
                        <div class="form-text text-muted small mt-1">Lütfen güncel bir tarih seçiniz.</div>
                    </div>
                    
                    <div class="mb-4">
                        <label asp-for="Notes" class="form-label fw-bold mb-2">
                            <span class="me-2">📝</span> Notlarınız (Opsiyonel)
                        </label>
                        <textarea asp-for="Notes" class="form-control custom-textarea" rows="3" placeholder="Antrenöre iletmek istediğiniz özel bir durum var mı?"></textarea>
                        <span asp-validation-for="Notes" class="text-danger small"></span>
                    </div>
                    
                    <div class="d-grid gap-3 mt-5">
                        <button type="submit" class="btn btn-primary btn-lg py-3 fw-bold">
                            <span class="me-2">🚀</span> Randevu Talebini Gönder
                        </button>
                        <a asp-action="Index" class="btn btn-outline-secondary py-2">
                            Randevu Listesine Dön
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<style>
    .appointment-form-card {
        border: 1px solid rgba(0, 245, 255, 0.2);
        background: var(--surface-primary);
        box-shadow: 0 15px 35px rgba(0, 0, 0, 0.4);
    }

    .header-icon {
        font-size: 3rem;
    }

    .gradient-text {
        background: linear-gradient(135deg, var(--neon-cyan), var(--neon-magenta));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        font-weight: 800;
        letter-spacing: 1px;
    }

    .custom-select, .custom-input, .custom-textarea {
        background-color: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(0, 245, 255, 0.2);
        color: white !important;
        padding: 0.75rem 1rem;
        transition: all 0.3s ease;
    }

    .custom-select:focus, .custom-input:focus, .custom-textarea:focus {
        background-color: rgba(255, 255, 255, 0.08);
        border-color: var(--neon-cyan);
        box-shadow: 0 0 10px rgba(0, 245, 255, 0.3);
        outline: none;
    }

    .custom-select option {
        background-color: #1a1b2e;
        color: white;
    }

    .btn-primary {
        background: linear-gradient(135deg, var(--neon-cyan), var(--electric-blue));
        border: none;
        box-shadow: 0 0 15px rgba(0, 245, 255, 0.4);
        transition: all 0.3s ease;
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 0 25px rgba(0, 245, 255, 0.6);
        background: linear-gradient(135deg, var(--electric-blue), var(--neon-cyan));
    }

    .glow-danger {
        background: rgba(255, 0, 110, 0.1);
        border: 1px solid rgba(255, 0, 110, 0.3);
        color: #ff006e;
    }

    /* Form transition animations */
    .form-label {
        color: var(--neon-cyan);
        font-size: 0.9rem;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    ::placeholder {
        color: rgba(255, 255, 255, 0.3) !important;
    }
</style>
