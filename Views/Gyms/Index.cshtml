@model IEnumerable<WebProgramlamaProje.Models.Gym>

@{
    ViewData["Title"] = "Spor Salonlarımız";
}

<div class="page-header mb-5">
    <div class="d-flex justify-content-between align-items-center">
        <div>
            <h1 class="display-5 fw-bold gradient-text mb-2">Spor Salonlarımız</h1>
            <p class="lead text-secondary">Modern ekipmanlarla donatılmış fitness merkezlerimiz</p>
        </div>
        @if (User.IsInRole("Admin"))
        {
            <a asp-action="Create" class="btn btn-primary">
                <span class="me-2">➕</span> Yeni Salon Ekle
            </a>
        }
    </div>
</div>

<div class="row g-4">
    @foreach (var item in Model)
    {
        <div class="col-md-6 col-lg-4">
            <div class="card gym-card h-100">
                <div class="gym-image-placeholder">
                    <div class="placeholder-content">
                        <span class="placeholder-icon">🏋️</span>
                        <span class="placeholder-text">Görsel Yok</span>
                    </div>
                </div>
                
                <div class="card-body d-flex flex-column">
                    <h4 class="card-title mb-3">@Html.DisplayFor(modelItem => item.Name)</h4>
                    
                    <div class="gym-info mb-3">
                        <div class="info-item">
                            <span class="info-icon">📍</span>
                            <span class="info-text">@Html.DisplayFor(modelItem => item.Address)</span>
                        </div>
                        <div class="info-item">
                            <span class="info-icon">📞</span>
                            <span class="info-text">@Html.DisplayFor(modelItem => item.PhoneNumber)</span>
                        </div>
                    </div>
                    
                    <div class="card-actions mt-auto">
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info btn-sm flex-fill">
                            <span class="me-1">👁️</span> Detaylar
                        </a>
                        
                        @if (User.IsInRole("Admin"))
                        {
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning btn-sm flex-fill">
                                <span class="me-1">✏️</span> Düzenle
                            </a>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm flex-fill">
                                <span class="me-1">🗑️</span> Sil
                            </a>
                        }
                    </div>
                </div>
            </div>
        </div>
    }
</div>

@if (!Model.Any())
{
    <div class="empty-state text-center py-5">
        <div class="empty-icon mb-3">🏋️</div>
        <h3 class="text-muted">Henüz Salon Eklenmemiş</h3>
        <p class="text-secondary">İlk spor salonunu eklemek için yukarıdaki butonu kullanın.</p>
    </div>
}

<style>
    .page-header {
        padding-bottom: 1.5rem;
        border-bottom: 1px solid rgba(0, 245, 255, 0.2);
    }

    .gradient-text {
        background: linear-gradient(135deg, var(--neon-cyan), var(--neon-magenta));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .gym-card {
        transition: all 0.3s ease;
        overflow: hidden;
    }

    .gym-image-placeholder {
        height: 200px;
        background: linear-gradient(135deg, var(--surface-primary), var(--surface-elevated));
        display: flex;
        align-items: center;
        justify-content: center;
        border-bottom: 1px solid rgba(0, 245, 255, 0.2);
    }

    .placeholder-content {
        text-align: center;
    }

    .placeholder-icon {
        font-size: 3rem;
        display: block;
        margin-bottom: 0.5rem;
        opacity: 0.5;
    }

    .placeholder-text {
        color: var(--text-muted-color);
        font-size: 0.9rem;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .card-title {
        color: var(--neon-cyan);
        font-weight: 700;
        font-size: 1.25rem;
    }

    .gym-info {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
    }

    .info-item {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.5rem;
        background: rgba(0, 245, 255, 0.05);
        border-radius: 6px;
        border-left: 2px solid var(--neon-cyan);
    }

    .info-icon {
        font-size: 1.25rem;
        flex-shrink: 0;
    }

    .info-text {
        color: var(--text-secondary-color);
        font-size: 0.9rem;
        line-height: 1.4;
    }

    .card-actions {
        display: flex;
        gap: 0.5rem;
        padding-top: 1rem;
        border-top: 1px solid rgba(0, 245, 255, 0.1);
    }

    .card-actions .btn {
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 0.6rem 1rem;
        font-size: 0.85rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .empty-state {
        background: rgba(36, 40, 66, 0.3);
        border: 2px dashed rgba(0, 245, 255, 0.2);
        border-radius: 12px;
        margin-top: 3rem;
    }

    .empty-icon {
        font-size: 4rem;
        opacity: 0.3;
    }

    /* Responsive */
    @@media (max-width: 768px) {
        .card-actions {
            flex-direction: column;
        }

        .card-actions .btn {
            width: 100%;
        }
    }
</style>
